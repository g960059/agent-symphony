{
  "name": "agent-symphony",
  "version": "0.1.0",
  "private": false,
  "description": "Local-first multi-agent orchestration kernel",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
